export enum GuideText {
  Check = "Check",
  CheckDescription = "The plugin will check if everything works correctly, the instruction will be closed and you will be redirected to start page.",
  FrontendSetup = "Frontend setup",
  ServerError = "Server error",
  ServerErrorDescription = "Go back to step 2 and verify your actions according to the instructions, if this does not help contact us via the report problem button.",
  ServerSetup = "Server setup",
}
